[pytest]
addopts = -ra
testpaths = tests
markers =
    unit: Focused unit-level behavior.
    integration: Cross-module or storage-backed behavior.
    e2e: End-to-end local workflow behavior.
    architecture: Architecture and layering contract checks.
    docs: Documentation and requirements contract checks.
    slow: Checks that are slower or rely on external tooling.
